initSidebarItems({"constant":[["EXP","Fixed RSA encryption exponent."]],"fn":[["decrypt","Decrypt the cipertext `msg` using the RSA private `key` and return the resulting plaintext."],["encrypt","Encrypt the plaintext `msg` using the RSA public `key` and return the ciphertext."],["genkey","Generate a pair of primes in the range `2**30..2**31` suitable for RSA encryption with exponent `EXP`. Warning: this routine has unbounded runtime; it works by generate-and-test, generating pairs of primes `p` `q` and testing that they satisfy `λ(pq) <= EXP` and that `λ(pq)` has no common factors with `EXP`."]]});